SELECT name, country
FROM Directors
INSERT INTO Person (name, country);

SELECT name, dob
FROM Stars
INSERT INTO Person (name, dob);

SELECT name, email
FROM Writers
INSERT INTO Person (name, email);
